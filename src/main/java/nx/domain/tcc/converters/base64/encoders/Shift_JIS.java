package nx.domain.tcc.converters.base64.encoders;

import nx.domain.tcc.Encoding;

public class Shift_JIS extends AbstractEncoder {
    public Shift_JIS() {
        super(Encoding.SJIS);
    }
}
