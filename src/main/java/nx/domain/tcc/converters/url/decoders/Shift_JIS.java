package nx.domain.tcc.converters.url.decoders;

import nx.domain.tcc.Encoding;

public class Shift_JIS extends AbstractUrlDecoder {
    public Shift_JIS() {
        super(Encoding.SJIS);
    }
}
