package nx.domain.tcc.converters.url.encoders;

import nx.domain.tcc.Encoding;

public class Shift_JIS extends AbstractUrlEncoder {
    public Shift_JIS() {
        super(Encoding.SJIS);
    }
}
